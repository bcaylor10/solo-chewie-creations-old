import { Header as MantineHeader } from "@mantine/core";

import './style.scss';

const Header = () => {
    return (
        <MantineHeader height={50}>
            :)
        </MantineHeader>
    )
};

export default Header;