import { Footer as MantineFooter } from "@mantine/core";

const Footer = () => {
    return (
        <MantineFooter height={200}>
            :(
        </MantineFooter>
    )
};

export default Footer;